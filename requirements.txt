# Dashboard runtime (what Streamlit Community Cloud installs).
# To rerun the modelling pipeline, install requirements-pipeline.txt instead.
pandas>=2.2,<3
numpy>=1.26
pyarrow>=15
cvxpy>=1.5
highspy>=1.7
streamlit>=1.40
plotly>=5.20
